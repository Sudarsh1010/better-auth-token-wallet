{
  "name": "better-auth-token-wallet-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "vp run -r build",
    "test": "vp test",
    "dev": "vp run -r dev",
    "lint": "vp check",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "vp run -r build && changeset publish"
  },
  "devDependencies": {
    "vite-plus": "^0.1.14",
    "typescript": "catalog:",
    "@changesets/cli": "^2.29.0",
    "vitest": "^3.0.0"
  }
}
